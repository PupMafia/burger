<h1>Welcome to Bob's Burgers!</h1>

<h2>Burgers up for grabs!</h2>

<ul>
    {{#each burgers}} {{#unless this.devoured}} {{> burgers/burger-block devoured=true}} {{/unless}} {{/each}}
</ul>

<h2>Gone all too soon!</h2>

<ul>
    {{#each burgers}} {{#if devoured}} {{> burgers/burger-block devoured=true}} {{/if}} {{/each}}
</ul>

<h2>Make-a-burg-omatic!</h2>
<form class="create-form">

    <div class="form-group">
        <label for="bu">Burgers:</label>
        <input type="text" id="bu" name="burger_name">
    </div>

    <div class="form-group">
        <label for="devoured">Up for grabs</label>
        <br>
        <input type="radio" name="devoured" value="1" checked> Already devoured!
        <br>
        <input type="radio" name="devoured" value="0"> Up for grabs
    </div>

    <button type="submit">Make Burger</button>

</form>